import { Container, Typography } from '@material-ui/core'

const ContactsPage = () => (
  <Container>
    <Typography variant='h4' component='h1' gutterBottom>Hello, I'm the Contact page</Typography>
  </Container>
)

export default ContactsPage
